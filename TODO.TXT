* TODO

    #A5 :vim :viki Cause for https://sourceforge.net/tracker/?func=detail&aid=2960771&group_id=108085&atid=649488
    #B5 :vim :viki Intervikis: :! ... always open in vim
    #C5 :vim :prototype Strict: Abstract: ''__abstract'' should be first set when it is queried?
    #C5 :vim :tplugin :TPlugin in interactive use can only access the current root directory but should be able to use any repository in whatever root directory (how to handle plugins with the same name in different root dirs?)
    #C5 :vim :tplugin Help tags should always be available.
    #C5 :vim :ttoc Sort order ([[plugin/ttoc.vim#TLet g:ttoc_world]])
    #C5 :vim :vikitasks Integration with calendar.vim
    #C5 :vim :worksheet cut/copy/paste item
    #C5 :vim :worksheet Delete item
    #D5 :vim :prototype prototype#Add(), prototype#Insert(): for objects with numeric fields à la |add()| and |insert()|
    #D5 :vim :prototype Strict: :PInterface NAME = PROTOTYPE
    #D5 :vim :prototype Strict: :PLet TYPE NAME = VALUE
    #D5 :vim :spec Handle embedded specs/tests?
    #D5 :vim :tlib tlib#agent#SwitchLayout(): switch between horizontal and vertical layout for the list
    #D5 :vim :tlib tlib#cache#Purge(): delete old cache files (for the moment use find)
    #D5 :vim :tlib tlib#file#Relative(): currently relies on cwd to be set
    #D5 :vim :tlib tlib#input#EditList(): Disable selection by index number
    #D5 :vim :tlib tlib#input#List(): List isn't updated on some occassions (eg tselectfiles + pick file per mouse) when resetting the state from an post-process agent
    #D5 :vim :tlib tlib#input#List(): Right Mouse -> Make commands accessible via popup-menu
    #D5 :vim :tlib tlib#input#List(): Some kind of command line to edit some preferences (sort etc.) on the fly
    #D5 :vim :tplugin \:TPluginUpdate command (for git and svn based repositories?)
    #D5 :vim :worksheet worksheet: ys ... Yank selected input fields (select via tlib#input#List)


% 2010-01-03; @Last Change: 2010-03-26.
% vi: ft=viki:tw=0:ts=4
