2009-02-21  Tom Link (micathom at gmail com)
	* 07tassert.vim: Incompatible changes.

2009-02-20  Tom Link (micathom at gmail com)
	* trag.vim (trag#AgentEditQFE): Use base index.
	* trag.vim (trag#RunCmdOnSelected): Use base index.

2009-02-15  Tom Link (micathom at gmail com)
	* tselectfiles.vim (:TSelectFiles, :TSelectFilesInSubdirs): Take an 
	initial pattern as the second optional argument.
	* scalefont.vim: BufExec & WinExec options.
	* tselectfiles.vim (tselectfiles#SelectFiles): 
	tselectfiles_filter_rx is always evaluated unless a pattern is 
	provided. tselectfiles_prefix is always evaluated.
	* tselectfiles.vim (tselectfiles#SelectFiles): if dir is &, search 
	&path.

2009-02-14  Tom Link (micathom at gmail com)
	* tmru.vim: If viminfo doesn't include '!', then use tlib to save 
	the file list.
	* vimball.rb: New command-line options: --no-helptags, --no-record, 
	--dir4vim.

2009-02-13  Tom Link (micathom at gmail com)
	* ttagecho.vim (ttagecho#Echo): :redraw before :echo.
	* rx.vim (tlib#rx#Escape): magic can be prefixed with a backslash.
	* tselectfiles.vim (:TSelectFilesInSubdirs): The optional argument 
	is interpreted as initial filter.
	* tselectfiles.vim (tselectfiles#BaseFilter): Changed split rx from 
	\A to [[:blank:][:digit:][:punct:][:space:]_].
	* World.vim (s:prototype.RestoreOrigin): Check also for &splitright.

2009-02-08  Tom Link (micathom at gmail com)
	* 07tassert.vim (s:GetSNR): Make sure backslashes match against 
	slashes.


vi: ft=changelog:tw=72:ts=4
